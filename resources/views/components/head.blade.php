<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
    <meta name="description" content="{{ __('Abra Shop') }} - {{ __('test task') }}"/>
    <meta name="author" content="{{ __('Evgeny Inatiev') }}"/>
    <title>{{ __('Abra Shop') }} - {{ __('test task') }}</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
    @vite('resources/css/app.css')
    @livewireStyles
</head>